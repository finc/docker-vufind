#!/bin/bash

# install phpunit
#wget -qO /usr/local/bin/phpunit https://phar.phpunit.de/phpunit.phar
wget -qO /usr/local/bin/phpunit https://phar.phpunit.de/phpunit-4.8.9.phar
chmod +x /usr/local/bin/phpunit
